---
- name: Single Event Rule
  hosts: localhost
  sources:
    - json_event_queue:
        file: "{{ json_data_file }}"
        delay: 2
        feedback: true
  rules:
    - name: r1
      condition: event.i < 5
      actions:
        - debug:
