---
- name: fail running a playbook
  hosts: all
  sources:
    - name: range
      range:
        limit: 5
  rules:
    - name:
      condition: event.i == 1
      action:
        run_playbook:
          name: playbooks/fail_playbook.yml
          ignore_errors: True
