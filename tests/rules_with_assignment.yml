---
- name: Demo rules multiple conditions all
  hosts: localhost
  sources:
    - name: range
      range:
        limit: 5
  rules:
    - name: assignment
      condition: events.event << fact.i == 0
      action:
        debug:
          events_event: "{{events.event}}"
